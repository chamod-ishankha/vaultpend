# VaultSpend is Firebase-first and no longer uses a self-hosted backend.
# This file is kept as a placeholder for optional local environment values.
# Copy to `.env` only if you introduce app-level non-secret variables.

# Example (optional):
# APP_ENV=development
